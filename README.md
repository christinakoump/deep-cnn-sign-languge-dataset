# deep-cnn-sign-languge-dataset
Trains a moderate CNN on image data, excluding 2 random classes during training. It applies Keras data augmentation, consists of 3 convolutional layers with pooling and 2 dense layers, runs for 10 epochs, and evaluates accuracy on the full test set including the hidden classes.
